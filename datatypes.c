#include <stdio.h>
#include <stdlib.h>

int main()
{
    // these three are the basic data types in c
    int age = 40;
    double height = 1.85;
    char grade = 'B'; // inside single quotes ''

    // modification of character data type
    // this is actually creating an array
    char pharse[] = "abcdefg hijklmnop qrs tuv wx yz";

    return 0;
}
